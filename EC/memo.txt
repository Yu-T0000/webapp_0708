# 読み取り
st.session_state.key
st.session_state["key"]

# 書き込み
st.session_state.key = value
st.session_state["key"] = value

ただしこの形式だとIDEの恩恵が受けられずにタイポに気付けない可能性がある

class StreamlitSessionManager:
    def __init__(self) -> None:
        self._session_state = st.session_state

    def get_user(self) -> User:
        return self._session_state["user"]

    def set_user(self, user: User) -> None:
        self._session_state["user"] = user

        SessionManager を用意


def __init__(self)って何？
コンストラクタ
StreamlitSessionManager().__init__()ってしなくても勝手に実行される部分
多分そんな感じ

session 
manager
↑↓     model
pages   →      ← (Mock)service
↑↓                  ↑↓
app                 DB